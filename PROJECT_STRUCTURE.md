# SimpleServer 项目文件结构说明

## 📁 项目目录结构

```
simpleServer/
├── src/                    # 源代码目录
│   └── main/
│       ├── java/           # Java源代码
│       │   └── com/example/simpleserver/
│       │       ├── controller/     # 控制器层
│       │       ├── service/        # 业务逻辑层
│       │       ├── repository/     # 数据访问层
│       │       ├── model/          # 实体模型层
│       │       ├── config/         # 配置类
│       │       └── SimpleServerApplication.java  # 启动类
│       └── resources/      # 资源文件
│           ├── application.properties  # 应用配置
│           └── db/         # 数据库脚本
├── target/                 # Maven构建输出目录
├── deploy/                 # 部署文件目录
│   ├── simpleServer-1.0.0.jar      # 可执行jar包
│   ├── application.properties      # 生产环境配置
│   ├── start-simpleServer.sh       # 启动脚本
│   └── health-check.sh             # 健康检查脚本
├── docs/                   # 文档目录
│   ├── API_DOCUMENTATION.md        # API接口文档
│   ├── PROJECT_SPECIFICATION.md    # 项目规范文档
│   └── README.md                   # 文档中心首页
├── scripts/                # 脚本工具目录
│   ├── run.bat/run.ps1             # 本地运行脚本
│   ├── build.bat/build.sh          # 项目构建脚本
│   ├── build-deploy-package.bat    # 部署包生成脚本
│   ├── cleanup-deploy.bat          # 部署目录清理脚本
│   ├── deploy-centos.sh            # CentOS部署脚本
│   ├── start-simpleServer.sh       # 服务器启动脚本
│   ├── health-check.sh             # 健康检查脚本
│   └── test-local.bat              # 本地测试脚本
├── test/                   # 测试文件目录
│   ├── test-task.json              # 单任务测试数据
│   ├── test-tasks-array.json       # 多任务测试数据
│   └── test_api.py                 # API测试脚本
├── apache-maven-3.8.6/     # 本地Maven安装目录
├── pom.xml                 # Maven项目配置文件
├── README.md               # 项目说明文档
├── .gitignore              # Git忽略文件配置
└── application-prod.properties     # 生产环境配置模板
```

## 📄 关键文件说明

### 核心配置文件
- **pom.xml**: Maven项目配置，定义依赖和构建规则
- **.gitignore**: Git版本控制忽略规则
- **application-prod.properties**: 生产环境配置模板

### 开发工具脚本 (scripts/)
- **run.bat/run.ps1**: 本地快速启动应用
- **build.bat/build.sh**: 项目编译和打包
- **build-deploy-package.bat**: 生成完整部署包
- **cleanup-deploy.bat**: 清理部署目录冗余文件
- **test-local.bat**: 本地功能测试

### 部署相关脚本 (scripts/)
- **deploy-centos.sh**: CentOS服务器完整部署脚本
- **start-simpleServer.sh**: 服务器应用管理脚本
- **health-check.sh**: 应用健康检查和自动恢复

### 测试文件 (test/)
- **test-task.json**: 单个任务测试数据
- **test-tasks-array.json**: 任务数组测试数据
- **test_api.py**: Python API接口测试脚本

## 🎯 使用说明

### 开发阶段
```bash
# 快速启动应用
scripts\run.bat

# 构建项目
scripts\build.bat

# 本地测试
scripts\test-local.bat
```

### 部署阶段
```bash
# 生成部署包
scripts\build-deploy-package.bat

# 清理部署目录
scripts\cleanup-deploy.bat
```

### 服务器运维
```bash
# 上传deploy目录到服务器后执行
chmod +x start-simpleServer.sh health-check.sh
./start-simpleServer.sh start
./start-simpleServer.sh status
```

## 📋 文件维护规范

### 新增文件原则
1. 配置文件放入对应目录（src/resources/, deploy/）
2. 脚本文件放入scripts/目录
3. 测试文件放入test/目录
4. 文档文件放入docs/目录

### 文件命名规范
- 脚本文件：使用kebab-case命名法
- 配置文件：使用snake_case命名法
- 文档文件：使用UPPER_SNAKE_CASE命名法

### 版本控制
- 只提交必要的源代码和配置文件
- 大型二进制文件和临时文件应加入.gitignore
- 部署包不纳入版本控制

---
📌 **注意**: 本项目采用标准Maven目录结构，所有开发相关文件与部署文件物理分离，便于维护和管理。